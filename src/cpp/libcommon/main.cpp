#include <stdio.h>
#include <iostream>
#include "Common.h"
#include <typeinfo>

using namespace _COMMON_NS;

using namespace std;

int main()
{   
/*
    CInteger i1(30), i2(20);

    if (i1 == i2)
    {
        std::cout << " Are Equal " << std::endl;
    }
    else
    {
        std::cout << " Not Equal " << std::endl;
    }

    std::function<std::string (Integer)> fnc = [](Integer a) { return std::to_string( a ); };

     CArrayList<Integer> list(16, fnc);

     for(int i =0; i < 32; i++)
     {       
         list.add( i );
     }

    std::function<Bool (Integer)> srchfnc = [](Integer a) { return (a == 5); };

     std::vector<Integer *> results = list.search( srchfnc );

     for(int i = 0; i < results.size(); i++)
     {
         Integer* ptr = results.at(i);

         std::cout << *ptr << std::endl;
     } 
 
     std::cout << "Random Order" << std::endl;

     list.shuffle();

     std::cout << list.toString() << std::endl;

     list.sort();
 
    std::cout << list.toString() << std::endl;

    list.sort(_DESCENDING);
 
    std::cout << list.toString() << std::endl;
*/
    return 0;
}
